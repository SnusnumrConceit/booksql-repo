mutation (
    $title: String!,
    $year: Int!,
    $description: String!
) {
    createBook(
        title: $title,
        year: $year,
        description: $description) {
            id
            title
            description
        }
}
